/**
 * Represents options for Disconomy
 * @property dailyAmount Amount per daily
 * @property weeklyAmount Amount for week
 * @property monthlyAmount Amount per month
 */
export interface DisconomyOptions {
    dailyAmount?: number;
    weeklyAmount?: number;
    monthlyAmount?: number;
}